import { SignNumberPipe } from './sign-number-pipe';

describe('SignNumbersPipe', () => {
  it('create an instance', () => {
    const pipe = new SignNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
